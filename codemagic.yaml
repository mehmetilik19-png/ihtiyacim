workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

    scripts:
      - name: Clean project
        script: |
          flutter clean
          rm -rf ios/Pods
          rm -rf ios/Podfile.lock
          rm -rf ~/Library/Developer/Xcode/DerivedData/*

      - name: Get dependencies
        script: flutter pub get

      - name: Reinstall pods
        script: |
          cd ios
          pod deintegrate
          pod setup
          pod install --repo-update
          cd ..

      - name: Remove bad G flag
        script: |
          find ios -type f \( -name "*.xcconfig" -o -name "*.pbxproj" -o -name "*.xcworkspace" \) -exec sed -i '' 's/-G//g' {} \;

      - name: Build iOS
        script: |
  flutter build ios --release --no-codesign --verbose 2>&1 | tee build.log
  grep -n "\-G" build.log || true

    artifacts:
      - build/ios/iphoneos/*.app
      - build/ios/iphoneos/*.ipa
