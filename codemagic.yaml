workflows:
  ios-release:
    name: iOS Release
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.ihtiyacim.ihtiyacim

    scripts:
      - flutter pub get
      - cd ios && pod install
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa
